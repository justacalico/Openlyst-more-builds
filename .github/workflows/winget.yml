name: Build Winget Repository

on:
  workflow_dispatch:
    inputs:
      calculate_hash:
        description: 'Calculate SHA256 hashes (slower but required for submission)'
        required: false
        default: true
        type: boolean

permissions:
  contents: write

concurrency:
  group: "winget-repo"
  cancel-in-progress: false

jobs:
  build-winget:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install PyYAML
      
      - name: Build Winget repository
        run: |
          if [ "${{ github.event.inputs.calculate_hash }}" = "true" ]; then
            python build_winget_repo.py \
              --output-dir winget-repo \
              --calculate-hash \
              --verbose
          else
            python build_winget_repo.py \
              --output-dir winget-repo \
              --verbose
          fi
      
      - name: Display build output
        run: |
          echo "=== Winget Repository Structure ==="
          find winget-repo/manifests -type f -name "*.yaml" | head -10
          echo ""
          echo "=== Generated Manifest Count ==="
          find winget-repo/manifests -name "*.yaml" | wc -l
          echo ""
          echo "=== Sample Manifest ==="
          find winget-repo/manifests -name "*.yaml" | head -1 | xargs cat || echo "No manifests generated"
      
      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      
      - name: Commit and push changes
        run: |
          git add winget-repo/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update Winget manifests $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
            # Try to push, if it fails due to conflicts, pull and retry
            if ! git push; then
              echo "Push failed, pulling latest changes and retrying..."
              git pull origin main --rebase
              git push
            fi
          fi